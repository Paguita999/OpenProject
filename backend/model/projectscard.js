import { BaseComponent } from '../libcomponents/base_component.js';

class projectscard extends BaseComponent {
    constructor() {
        super();
    }

    connectedCallback() {
       const id = this.getAttribute;
       const identifier=identifier.getAttribute(projectemodel-identifier);
       const name=name.getAttribute(projectemodel-name);
       const description=this.description.getAttribute(projectemodel-description);
       const createdAt=this.createdAt.getAttribute(projectemodel-createdAt);
       const status=this.status.getAttribute(projectemodel-status);
       const statusExplanation=this.statusExplanation.getAttribute(projectemodel-this.statusExplanation);
    }
}