import { BaseComponent } from '../libcomponents/base_component.js';

class projectscard extends BaseComponent {
    constructor() {
        super();
    }

    connectedCallback() {
       const id = this.getAttribute;
       const identifier=identifier.getAttribute(projectemodel-identifier);
       const name=name.getAttribute(projectemodel-name);
       const description=this.description.getAttribute(projectemodel-description);
       const createdAt=this.createdAt.getAttribute(projectemodel-createdAt);
       const status=this.status.getAttribute(projectemodel-status);
       const statusExplanation=this.statusExplanation.getAttribute(projectemodel-this.statusExplanation);
    }

    connectedCallback() {
        const derivedDate=derivedDate.getAttribute(work_packages-derivedDate);
        const derivedDueDate=derivedDueDate.getAttribute(work_packages-derivedDueDate);
        const _type=_type.getAttribute(work_packages-_type);
        const id=id.getAttribute(work_packages-id);
        const lockVersion=lockVersion.getAttribute(work_packages-lockVersion);
        const subject=subject.getAttribute(work_packages-subject);
        const description=description.getAttribute(work_packages-description);
        const scheduleManually=this.scheduleManually.getAttribute(work_packages-scheduleManually);
        const startDate=startDate.getAttribute(work_packages-startDate);
        const dueDate=dueDate.getAttribute(work_packages-this.dueDate);
        const estimatedTime=estimatedTime.getAttribute(work_packages-stimatedTime);
        const derivedEstimatedTime=derivedEstimatedTime.getAttribute(work_packages-derivatedEstimatedTime);
        const derivedRemaniningTime=derivedRemaniningTime.getAttribute(work_packages-derivedRemaniningTime);
        const duration=duration.getAttribute(work_packages-duration);
        const ignoreNonWorkingDays=ignoreNonWorkingDays.getAttribute(work_packages-ignoreNonWorkingDays);
        const percentageDone=percentageDone.getAttribute(work_packages-percentageDone);
        const derivedPercentageDone=derivedPercentageDone.getAttribute(work_packages-derivedPercentageDone);
        const createdAt=createdAt.getAttribute(work_packages-createdAt);
        const updatedAt=updatedAt.getAttribute(work_packages-updatedAt);
    }
}